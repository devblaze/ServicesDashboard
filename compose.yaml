services:
  database:
    image: postgres:16
    environment:
      POSTGRES_DB: servicesdashboard
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin123
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U admin -d servicesdashboard"]
      interval: 30s
      timeout: 10s
      retries: 3

  servicesdashboard:
    build:
      context: ./ServicesDashboard
      dockerfile: Dockerfile.dev
    ports:
      - "5000:5000"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_URLS=http://+:5000
      - ConnectionStrings__DefaultConnection=Host=database;Database=servicesdashboard;Username=admin;Password=admin123
    volumes:
      - ./ServicesDashboard:/app
      - /app/bin
      - /app/obj
    depends_on:
      database:
        condition: service_healthy

  frontend:
    platform: linux/amd64
    build:
      context: ./services-dashboard-frontend
      dockerfile: Dockerfile.dev
    ports:
      - "5173:5173"
    volumes:
      - ./services-dashboard-frontend:/app
      - /app/node_modules
    environment:
      # Use OrbStack domain
      - VITE_API_BASE_URL=http://servicesdashboard.servicesdashboard.orb.local
      - VITE_API_URL=http://servicesdashboard.servicesdashboard.orb.local/api
    command: ["npm", "run", "dev", "--", "--host", "0.0.0.0"]

volumes:
  postgres_data: