# Docker Hub Configuration
# Replace 'yourusername' with your actual Docker Hub username
DOCKER_USERNAME=yourusername

# Version tag for Docker images (e.g., 0.0.3, latest)
VERSION=latest

# Database Configuration
DB_USER=admin
DB_PASSWORD=admin123
# PostgreSQL port exposed to host (default: 5432)
DB_PORT=5432

# Backend Configuration
# Port exposed to the host machine for the backend API
BACKEND_PORT=5050
# Internal port inside the container (usually keep at 8080 for production)
BACKEND_INTERNAL_PORT=8080

# Frontend Configuration
# Port exposed to the host machine for the frontend web app
FRONTEND_PORT=5173
# Internal port inside the container (usually keep at 80 for nginx)
FRONTEND_INTERNAL_PORT=80

# Ollama Configuration (optional, for AI features)
# Use host.docker.internal on Docker Desktop, or the host IP address on Linux
OLLAMA_URL=http://host.docker.internal:11434
